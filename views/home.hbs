<!DOCTYPE html>
<html>
    <head>
        <title>{{title}}</title>
    </head>
    <body>
        <form action="/" method="POST">
            <input name="newtask">
            <button type="submit">ADD</button>
        </form>
        <ul>

            {{#each tasks as |task|}}
                <li>{{task}}</li>
            {{/each}}
        
            <!-- {{tasklist}} was not formatting correctly -->
            
        </ul>
    </body>
</html>